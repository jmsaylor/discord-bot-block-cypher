module.exports = {
    name: 'mention',
    usage: 'metion',
    description: '',
    run: async ({ msg }) => {
        // console.log(typeof msg.mentions.users);
        // msg.reply(typeof msg.mentions);
    },
};
